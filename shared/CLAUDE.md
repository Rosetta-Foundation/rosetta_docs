# shared

Shared assets used across multiple Rosetta repos.

This folder holds cross-repo shared resources (schemas, fixtures, config, or extracted shared
packages) as the platform grows. Keep it minimal — extract here only when something is genuinely
consumed by more than one repo.
